import React from "react";

const Settings = ({}) => {
  return (
    <div>
      <h2>Settings</h2>
      <p>
        This is still work in progress.
      </p>
    </div>
  );
};

export default Settings;
